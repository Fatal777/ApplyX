import { InterviewRoom } from '@/components/interview';

/**
 * InterviewRoomPage
 * Wrapper page for the interview room component
 */
const InterviewRoomPage = () => {
  return <InterviewRoom />;
};

export default InterviewRoomPage;
