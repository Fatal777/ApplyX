[settings]
default = zyte_scrapers.settings

[deploy]
url = https://app.zyte.com/api/scrapyd/
project = applyx
# Note: Add your Zyte API key when deploying
# Via shub: shub login (enter API key)
# Via GitHub Actions: Add ZYTE_API_KEY to repository secrets
